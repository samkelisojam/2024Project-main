@model ConsultantViewModel

<div class="container mt-5">
    <h3 class="mb-4">
        <i class="fas fa-user-clock"></i> User Login Sessions - @Model.SelectedUser.Email
    </h3>
    <table class="table table-striped">
        <thead>
            <tr>
                <th scope="col"><i class="fas fa-wallet"></i> Account No</th>
                <th scope="col"><i class="fas fa-user"></i> Name</th>
                <th scope="col"><i class="fas fa-envelope"></i> Email</th>
                <th scope="col"><i class="fas fa-id-card"></i> Id Number</th>
                <th scope="col"><i class="fas fa-calendar-day"></i> Recent Login</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var login in Model.loginSessions)
            {
                <tr>
                    <td>@Model.SelectedUser.AccountNumber</td>
                    <td>@(Model.SelectedUser.FirstName + " " + Model.SelectedUser.LastName)</td>
                    <td>@Model.SelectedUser.Email</td>
                    <td>@Model.SelectedUser.IDnumber</td>
                    <td>@login.TimeStamp</td>
                </tr>
            }
        </tbody>
    </table>
</div>

<!-- Ensure you include Font Awesome or another icon library in your project -->
<script src="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/js/all.min.js" integrity="sha384-n8DsbA5h8xHGzM2eq0lV9LgDffI4AbNfPY7WcbR5nPU8d6E6lA9FvxgZBcr7EqU" crossorigin="anonymous"></script>
